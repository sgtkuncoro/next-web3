import { Card } from "antd";
import React from "react";

const Swap = (props: any) => {
  return (
    <div className="site-card-border-less-wrapper max-w-xl m-auto">
      <Card bordered={false}></Card>
    </div>
  );
};

export default Swap;
